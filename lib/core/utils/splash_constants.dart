/// Zentrale Konstanten für die SplashPage-Animation
class SplashConstants {
  static const double imageSize = 300;
  static const double initialOpacity = 1.0;
  static const double finalOpacity = 0.0;
  static const Duration fadeOutDuration = Duration(milliseconds: 5000);
  static const Duration splashDelay = Duration(milliseconds: 4000);
  static const Duration fadeTransitionDuration = Duration(milliseconds: 1500);
  static const Duration fadeOutWait = Duration(milliseconds: 500);
}
