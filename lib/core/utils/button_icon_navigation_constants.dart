import 'package:flutter/material.dart';

/// Zentrale Konstanten für das ButtonIconNavigation-Widget
class ButtonIconNavigationConstants {
  static const EdgeInsetsGeometry padding = EdgeInsets.symmetric(
    horizontal: 24,
    vertical: 12,
  );
  static const TextStyle textStyle = TextStyle(
    fontSize: 18, // Standardgröße, kann überschrieben werden
    color: Colors.white,
    fontWeight: FontWeight.normal,
  );
  static const double iconSize = 20;
}
