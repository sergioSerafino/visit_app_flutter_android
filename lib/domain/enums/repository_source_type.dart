// /domain/enums/repository_source_type.dart

// sollte nicht in den Repositories selbst stehen
// in einer globalen App-Konfiguration oder DI-Container
enum RepositorySourceType { api, mock, local }
